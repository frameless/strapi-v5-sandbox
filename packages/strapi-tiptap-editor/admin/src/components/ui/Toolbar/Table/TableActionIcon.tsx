import styled from 'styled-components';
import { Box } from '@strapi/design-system';

export const TableActionIcon = styled(Box)`
  color: inherit;
  font-size: ${({ theme }) => theme.fontSizes[4]};
`;
